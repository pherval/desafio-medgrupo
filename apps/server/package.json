{
  "name": "server",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port $PORT",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pleiva <npmjs@peter.dev.br>",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
